# 2026-07-05 Daily Notes

## 12:00 UTC — ButterflAI Agent Eval (Cron)

**Issue found: coord-loop.js crashing every 5 minutes**

Error: `[coord] tick error: desires.getPendingSocialDesires is not a function`
- Running continuously since ≥10:35 UTC
- Root cause: `desires.js` does not export `getPendingSocialDesires`; function exists in `db.js`
- Impact: coordinator tick fully failing — social desires not processed, recurring templates not checked
- Fix: export from `desires.js` OR change `coord-loop.js` line 139 to call `db.getPendingSocialDesires(user_id)` directly

Site is up (200 OK). No agent failures. /inbound 403 is expected (Twilio sig validation).

Note: webchat notification to Sean failed — bot not a member of the supergroup chat (403). Sean needs to be notified via another channel.
