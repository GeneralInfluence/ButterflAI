# Memory — 2026-07-17

## Session: Privacy infrastructure + debugging (00:00–00:13 UTC)

### What we built
- **PRIVACY.md** — constitutional document, 7 invariants, each tied to a test
- **`sensitive.js`** — AES-256-GCM encrypted private store (`user_private_data`), sensitivity classifier, per-recipient consent model, access audit log, sensitive mode flag
- **Migration 026** — `user_private_data` + `private_data_access_log` tables
- **Migration 027** — `sensitive_mode INTEGER` column on `user_preferences` (persists across restarts)
- **`store_private_data` tool** — separate agent tool for sensitive data; `update_preferences` now rejects sensitive content with `SENSITIVE_DATA_DETECTED`
- **🔒 private mode button** — in chat UI, banner, placeholder change, notifies server
- **`privacy.test.js`** (23 tests) + **`privacy-api.test.js`** (5 tests)
- **Audit log 403 fix** — `/api/user/:userId/audit` was wide open (no auth). Real vulnerability, fixed with `requireAuth` + userId check.
- **"Thinking…" status on send** — `sse.push(userId, { role: 'status', text: 'Thinking…' })` pushed immediately from `/api/chat/send` handler before agent picks up
- **Silent failure fix** — agent `tick()` now logs full stack on error + pushes user-facing "Sorry, I hit a snag" to webchat instead of going silent
- **Sensitive mode persisted in DB** — was in-memory Set (lost on restart), now `user_preferences.sensitive_mode`

### Debug findings
- **`before()` at file scope + `describe()` blocks = hang in node:test** — `before()` hooks must be INSIDE `describe()` blocks, not at file top level
- **`webapp-auth` exports `issueToken`, NOT `signToken`** — integration tests must use OTP flow (`/auth/otp/send` → `/auth/otp/verify` → `set-cookie`) to get auth cookies
- **Process hang after failed supertest tests** — failed tests leave HTTP connections open; with --test-timeout the process eventually exits but tests look like they timed out. Fix: get the tests passing first.
- **Silent message drop in prod (00:00 UTC)** — Sean sent message after toggling 🔒 button. Message was marked `processed=1` but no `[agent] processing` log and no response in `conversation_history`. Root cause unclear (likely sensitive module interaction). Injected recovery message manually via SSH. Fixed with better error logging and user-facing error reply.
- **Sensitive mode reset on restart** — was in-memory only. Sean toggled ON, then a deploy reset it. Fixed by persisting to DB (migration 027).

### Commits this session (newest first)
- `89285bb` — Thinking status + silent failure fix + migration 027
- `88f91b1` — PRIVACY.md + sensitive.js + privacy tests + audit log 403 fix (28 new tests)

### Current test count: 382 tests, 0 failures

### Production state
- **Sean's last message** had no response — recovery message injected: "Got it, private mode is on. What would you like me to store securely?"
- **Sensitive mode** for Sean: `false` in prod (reset during deploy; now persisted going forward)
- **Prod URL**: https://butterflai.social / https://butterflai.fly.dev

### Key identifiers (carry forward)
- Sean: `3a2ec4cc-7a6b-46a4-ba06-a067c4dfc44d`, `+16099233179`
- Bam Bam: `4cb60ef9-8d61-4cd8-8f59-63fb1f652b36`, `+12026567811`
- Allie: `03cace6d-86a4-4404-9778-fa2ea0028c9a`, `+15702677976`
- 80s bar event: `e6604283-19ed-4c04-b356-a2f9083d9fcd`
- Fly machine: `876430a03623d8`, region `ewr`
- flyctl: `/home/node/.fly/bin/flyctl`, token env: `FLY_DEPLOY_TOKEN`
- Repo: `/home/node/clawd/workspace/butterflai-repo`

### Open TODOs (carry forward)
- [ ] Set lat/lng for Sean and Allie user rows (location routing broken)
- [ ] Set Sean's timezone to America/New_York in DB (still null)
- [ ] Confirm chat scroll fix on device
- [ ] Confirm markdown bold rendering in chat
- [ ] Push notifications end-to-end (Sean to tap Allow in PWA)
- [ ] Wire GitHub secret FLY_API_TOKEN for CI auto-deploy
- [ ] Fix `desires.getPendingSocialDesires is not a function` (pre-existing, no user impact)
- [ ] Google OAuth app verification for public calendar connect
- [ ] Wire `sensitive.isSensitiveMode(userId)` check into agent message-processing loop
- [ ] Create `web/tests/integration/privacy-api.test.js` API tests for invariant 5 (SMS self-ID via supertest) — partially done but more coverage needed
- [ ] `db._raw()` used in `sensitive.js` — verify exported correctly (seems to work in prod)
