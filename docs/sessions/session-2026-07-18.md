# Memory — 2026-07-18

## ⏭️ NEXT SESSION — START HERE (resume the agent-training work)

**Goal:** rapid-iterate the base user agent's behavior using the new multi-agent
simulator, running on **Haiku 4.5**. Blocked only on one thing: the Anthropic key
must be present in the runtime.

**Do this, in order:**
1. **Add the key to this environment.** Claude Code web → click the cloud icon
   (environment selector) → hover this environment → gear/settings → **Environment
   variables** (`.env` format, no quotes) → add:
   ```
   ANTHROPIC_API_KEY=sk-ant-...
   ```
   (Optionally `AGENT_MODEL=claude-haiku-4-5-20251001` too.) There's no dedicated
   secrets store yet — env vars are visible to anyone who can edit the environment.
   Env vars apply at session start, so **start a fresh session** after saving.
   Do NOT paste the key into chat.
2. **Verify Haiku answers** with a tiny real call (Anthropic is on the proxy
   allowlist): a one-shot `anthropic.messages.create({model:'claude-haiku-4-5-20251001', max_tokens:16, messages:[{role:'user',content:'ping'}]})`.
3. **Run the sim live:** `cd web && npm run sim`, then try the scenarios below.
   Watch the cascade + tool calls. When Haiku does something wrong, fix the rule
   in `buildSystemPrompt()` (`web/agent.js`), add an assertion in
   `tests/integration/system-prompt.test.js`, rerun. That's the training loop.

**First training scenarios to throw at Haiku (Aphilos ET, BamBam PT):**
- `aphilos: invite bambam to dinner friday 7pm` → expect create_social_event →
  message_agent BamBam → BamBam surfaced the invite → RSVP flows back.
- `bambam: yeah im in` → expect confirm_coordination_invite, host notified.
- `aphilos: what's bambam's status?` → must read live state, not re-ask.
- `aphilos: set up a group hang this weekend, invite bambam` (vague time) → must
  ask for a time, never invent 7pm.
- `bambam: actually can't make friday` (as a pure contact reply path) → RSVP/relay.
- Watch for: fabricated confirmations, inventing times, leaking one user's words
  to the other's agent, over-asking instead of checking state.

---

## Branch / state (all pushed)

- **Branch:** `claude/contacts-sms-routing-rewrite-getxcl` (PR #9 already MERGED +
  deployed; branch was restarted from `main` for the simulator follow-up — this is
  a NEW PR's worth of work, not the merged one). Head commit `19fd938`.
- **Full suite: 451 tests passing.** Run `cd web && npm test` before any commit.
  (Fresh clone needs deps: `NODE_ENV=development npm install` then
  `npx prebuild-install --download` for better-sqlite3.)

### Shipped this session
1. **Phase 1 — contacts SMS-routing rewrite (MERGED to main, live in prod).**
   - `routeInboundSms(from, body)` in `server.js`: one user + one contact lookup,
     ordered dispatch (STOP|START|opt-out|RSVP|pure-contact relay|onboarding|user).
   - **Fixed the contact-reply dead-end:** non-user contact SMS used to be stored
     `from_type='contact'` then dropped (getUser(from_id) fails). Now
     `resolveContactRelay()` (`agent.js`) relays it to the event host (7-day
     window) or the inviter; host agent handles it. `RELAYED CONTACT MESSAGE`
     system-prompt rule added.
   - **Dual-identity Rule B (owner sign-off):** a phone that's both a user AND a
     contact is ALWAYS handled by its own agent, even for RSVPs
     (`contact = user ? null : getContactByPhone(from)`). Invariant: a registered
     user's words never route to another user's agent.
   - Hardening: `getContactByPhone` deterministic (ORDER BY updated_at DESC, id
     DESC LIMIT 1); coord-loop stopped writing `from_type='system'`/
     `channel='coord_notify'` (violated inbound_messages CHECK) → now
     `from_type='user'`/`channel='agent'`.
2. **Multi-agent simulator (this branch, not yet PR'd).**
   - `web/tools/sim.js`: seeds two full users (Aphilos +15550000001 ET, BamBam
     +15550000002 PT, mutual contacts, prefs), stubs Twilio via `sms._setClient`,
     feeds input through the REAL `routeInboundSms` + drains the whole
     inbound_messages queue (agent↔agent cascade) printing every hop + tool call.
     REPL: `aphilos:`/`bambam:` (or `a:`/`b:`), `/state /events /a2a /prompt a|b
     /reseed /help /quit`. In-memory DB (override `SIM_DB_PATH`).
   - **Pinned to Haiku 4.5** (`CURRENT_MODEL='claude-haiku-4-5-20251001'` in
     sim.js, set into `process.env.AGENT_MODEL` BEFORE requiring agent.js).
   - **Requires a real key** — refuses to run a turn without `ANTHROPIC_API_KEY`
     (never stubs Claude). Boot + inspectors work keyless.
   - Two prod-safe no-op hooks added to `agent.js`: `_setAnthropic(client)` and
     `_setToolObserver(fn)` (inert unless set; used only by sim + smoke test).
   - `web/tests/integration/sim-engine.test.js`: drives the engine with a scripted
     Anthropic stub (dummy key, no real calls) — proves drain loop + a2a cascade +
     SMS capture. `npm run sim`. Docs: `web/tools/README.md`.

### Commits this session (newest first)
- `19fd938` sim: pin model to Haiku 4.5
- `802320c` sim: require real key + pin current model (no fake turns)
- `5975da8` add multi-agent simulator
- `9edea6a` dual-identity Rule B
- `bb58997` Phase 1 routing rewrite + contact-reply dead-end fix
- (merged to main as PR #9, commit dffd5d4 — CI green, Fly deploy succeeded)

### Open decisions / notes
- Confirm prod's actual `AGENT_MODEL` Fly secret. June notes said
  `claude-sonnet-4-6`; sim is pinned to Haiku 4.5 per owner request for this
  training run. If prod differs, that's fine — sim overrides via AGENT_MODEL.
- Simulator branch still needs a PR opened when ready (owner hasn't asked yet).
- Possible richer follow-up: relayed contact replies could be answered directly
  to the contact (currently relayed to host agent). Deferred; owner set
  routing-layer-only scope.

### Key identifiers
- Sim users: Aphilos `sim-aphilos` +15550000001 (America/New_York); BamBam
  `sim-bambam` +15550000002 (America/Los_Angeles).
- Model for training: `claude-haiku-4-5-20251001`.
- Prod (real): https://butterflai.social — routing changes are LIVE as of this session.
