# 2026-06-20 — ButterflAI Build Session

## Users on platform
- **Sean** (Aphilos) — `3a2ec4cc`, phones: +16099233179 (primary), +12026567811 (merged in)
- **Allie** — `03cace6d`, +15702677976
- **Melanie** — `d13aa799`, +17759546191 (joined today; sent contacts import link)

## Work done this session

### Per-user SQLite split — REVERTED
- Attempted to split single `butterflai.sqlite` into `main.sqlite` + `users/{id}.sqlite`
- Caused ~2h downtime (3 consecutive deploy crashes)
- Root causes: schema path wrong in Docker, `SQLITE_READONLY` from partial migration, `timezone` column mismatch
- Reverted to `cab39fa` baseline. All data intact.
- Migration scripts and schemas committed but not active. Revisit with: local test on prod DB copy → volume snapshot → migrate.

### Admin router (`/admin`) — LIVE
- Auth: `X-Admin-Secret` header (secret: `25ff39ccb2369732d68a2ab42b53defa60172a1803b31657d945127236fd3312`)
- Endpoints: `GET /admin/stats`, `GET /admin/users`, `GET /admin/users/:id`, `POST /admin/users/merge`, `DELETE /admin/users/:id`
- Used to merge Sean's two Aphilos accounts (kept `3a2ec4cc`, merged 10 msgs from `eb51341a`)

### Web app UI — LIVE at `/app`
- Login: phone OTP → JWT httpOnly cookie (30d), `POST /auth/otp/send` + `/auth/otp/verify`
- Chat: iMessage-style, SSE for real-time agent push, history loads on open
- Contacts: searchable list with tier badges
- Pages: `/app/login`, `/app/chat`, `/app/contacts`
- Subdomain cert added: `app.butterflai.social` → DNS A: `66.241.124.235`, AAAA: `2a09:8280:1::119:c881:0` (DNS not yet added by Sean)
- Bug fixed: `jsonwebtoken` missing from `package.json` → crashed all auth. Fixed + redeployed.
- SSE auth fix: 401 now sends redirect event instead of hanging EventSource

### Agent web tools — LIVE
- `fetch_url`: reads any URL user sends (HTTP GET, strips HTML, 6k char cap)
- `web_search`: Brave Search API — needs `BRAVE_SEARCH_API_KEY` secret (not yet set)

### User location — LIVE
- Migration 007: `city`, `region`, `country` columns on users
- Migration 008: `lat`, `lng`, `location_updated_at` columns on users
- Onboarding: new step `awaiting_location` between name and contacts; seeds city from area code
- Web app: 📍 button in chat header, prompts if no location stored, POSTs to `/api/user/location`
- Reverse geocode: Nominatim (OpenStreetMap, free, no key)
- Agent tools: `get_user_location` (returns city + lat/lng), `set_user_location`
- `areacode.js`: area code → city lookup table

## Secrets on Fly
- `ADMIN_SECRET`: `25ff39ccb2369732d68a2ab42b53defa60172a1803b31657d945127236fd3312`
- `JWT_SECRET`: `(set, not logged)`
- `BRAVE_SEARCH_API_KEY`: NOT SET — web_search will return "not configured"

## Git commits today (main)
- `edcefa1` revert: per-user SQLite split
- `2e71105` feat: admin router
- `9bbc4e6` feat: web app UI (login, chat, contacts, SSE, OTP)
- `d632002` feat: agent fetch_url + web_search tools
- `9e40cbb` feat: user location (onboarding step, area code seed, agent tools)
- `78a8b3c` feat: GPS location sharing (web app 📍, Nominatim reverse geocode)
- `fd1b38e` fix: jsonwebtoken in package.json; SSE auth redirect fix

## Open issues
- `web_search` needs `BRAVE_SEARCH_API_KEY` Fly secret
- DNS for `app.butterflai.social` not yet added (Sean needs to add A/AAAA records)
- Coord-loop error: `desires.getPendingSocialDesires is not a function` — pre-existing, not user-facing
- Per-user SQLite split: planned pre-public-launch, needs safer migration procedure
- Sean tried web app login — was stuck (jsonwebtoken crash). Fixed. Needs re-test.
- Melanie needs to import her contacts (link sent via SMS)
